import React from 'react';
import styles from './notFoundBlock.module.scss';

function NotFoundBlock() {
  return <h1 className={styles.title}>Ничего не найдено :(</h1>;
}

export default NotFoundBlock;
